#ifndef RANK_TABLE_H
#define RANK_TABLE_H

#define thresholdLevel(level)       (level * 2)
#define maxHealthIncrease(level)    (5 + level * 2)
#define attackIncrease(level)       (level - 1)

#endif /* RANK_TABLE_H */